#include <stdio.h>
/**
 * main - Function that  prints the alphabet in lowercase.
 *
 * Return: 0 (Successful)
 */
int main(void)
{
	l = 'a'
	while (l <= 'z')
	{
	putchar(l);
	l++;
	}
	putchar('\n');
	return (0);
}
