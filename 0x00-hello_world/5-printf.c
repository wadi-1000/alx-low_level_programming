#include <stdio.h>
/**
 * main - Function that prints a line using printf
 * Return: 0 if successful
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
