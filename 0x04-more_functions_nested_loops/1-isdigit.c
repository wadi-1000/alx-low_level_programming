#include "main.h"

/**
 * int _isdigit - Function that checks for a digit 0 through 9
 * @c: The character to be tested
 *
 * Return: 0 (Successful)
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (0);
	}
}
